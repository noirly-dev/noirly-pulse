import { EmptyState } from "@/src/components/EmptyState";

export default function WorkspaceSettingsPage() {
  return (
    <EmptyState
      title="Workspace settings"
      description="Rename, archive, and transfer ownership arrive in Phase 2."
    />
  );
}
